{
  "name": "patronum-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply patronum --remote",
    "migrate:local": "wrangler d1 migrations apply patronum --local",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "wrangler": "^4.0.0"
  }
}
